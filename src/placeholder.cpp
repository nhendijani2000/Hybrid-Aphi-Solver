// Placeholder translation unit so the aphi_core library has something to compile
// while the real source tree (mesh, assembly, gauge, solve, fields) is built out.
// Delete once real .cpp files exist under src/.

namespace aphi_solver {
// intentionally empty
}
